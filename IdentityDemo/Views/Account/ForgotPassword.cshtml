@model ForgotPasswordViewModel

<div class="container">
    <div class="row justify-content-center align-items-center min-vh-100">
        <div class="col-md-6 col-lg-5">
            <div class="card border-0 shadow-lg dark-card light-card">
                <div class="card-body p-5">
                    <h2 class="text-center mb-4 dark-text">Reset Password</h2>
                    <form method="post" asp-action="forgotpassword">
                        <div asp-validation-summary="All" class="text-danger mb-3 small"></div>
                        
                        <div class="form-floating mb-4">
                            <input asp-for="Email" class="form-control dark-input light-input shadow-sm" placeholder="name@example.com" />
                            <label asp-for="Email" class="dark-label light-label"></label>
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>

                        <button type="submit" class="btn btn-primary w-100 mb-3 py-2 shadow-sm dark-btn light-btn">Send Reset Link</button>
                    </form>

                    <div class="text-center mt-4">
                        <a asp-action="login" class="text-decoration-none dark-link light-link">Back to login</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}

@section Styles {
    <link rel="stylesheet" href="~/css/auth-light.css" />
    <link rel="stylesheet" href="~/css/auth-dark.css" />
}